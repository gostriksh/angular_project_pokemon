import { ScrollFocusBottomDirective } from './scroll-focus-bottom.directive';

describe('ScrollFocusBottomDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollFocusBottomDirective();
    expect(directive).toBeTruthy();
  });
});
