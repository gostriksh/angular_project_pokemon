import { DisplayDamageDirective } from './display-damage.directive';

describe('DisplayDamageDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayDamageDirective();
    expect(directive).toBeTruthy();
  });
});
