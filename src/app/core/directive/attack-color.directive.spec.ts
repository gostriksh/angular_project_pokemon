import { AttackColorDirective } from './attack-color.directive';

describe('AttackColorDirective', () => {
  it('should create an instance', () => {
    const directive = new AttackColorDirective();
    expect(directive).toBeTruthy();
  });
});
