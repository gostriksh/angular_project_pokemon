import { HealthBarDirective } from './health-bar.directive';

describe('HealthBarDirective', () => {
  it('should create an instance', () => {
    const directive = new HealthBarDirective();
    expect(directive).toBeTruthy();
  });
});
